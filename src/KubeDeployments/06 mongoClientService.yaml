apiVersion: v1
kind: Service
metadata: 
  name: mongoclientservice
  labels: 
    name: mongoclientservice
spec: 
  type: NodePort
  ports:
    # the port that this service should serve on
    - port: 3000
  # label keys and values that must match in order to receive traffic for this service
  selector: 
    app: mongoClient